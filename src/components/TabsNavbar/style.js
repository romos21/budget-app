import {makeStyles} from "@mui/styles";

const useStyles = makeStyles((theme) => ({
    Tabs: {
        marginBottom: "40px",
    },
    Tab: {
        fontSize: "20px",
        fontWeight: 600,
        textTransform: "none",
    }
}));

export default useStyles;
